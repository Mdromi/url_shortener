<h1> URL Shortener </h1>
<%= render partial: 'form', locals: {link: @link} %>
<turbo-frame id="links">
  <div id="" class="mt-8 flex flex-col divide-y divide-gray-100">
    <%= render @links%>
  </turbo-frame>
</div>
